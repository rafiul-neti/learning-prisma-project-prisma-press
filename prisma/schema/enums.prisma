// users enums
enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  USER
  ADMIN
  AUTHOR
}

// posts enums
enum PostStatus {
  DRAFT
  PUBLISHED
  ARCHIEVED
}

// comments enums
enum CommentStatus {
  APPROVED
  REJECTED
}
